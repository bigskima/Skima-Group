# Mobile App

The mobile app will be created after the reusable platform foundation and frontend design system
exist.

No business module screens belong in Milestone 1.

When mobile authentication starts, use `packages/auth` with only `SUPABASE_URL` and
`SUPABASE_ANON_KEY`.
