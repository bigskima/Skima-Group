import * as Linking from "expo-linking";
import { router } from "expo-router";
import { Bell, BellRing, CheckCircle2, ChevronRight } from "lucide-react-native";
import { useState } from "react";
import { ActivityIndicator, Pressable, RefreshControl, StyleSheet, Text, View } from "react-native";
import { domainQueries } from "../api/domains";
import { displayStatus, firstString, nestedRecord, recordId } from "../api/records";
import { enableNotifications } from "../notifications/useNotificationLifecycle";
import { colors, radii, spacing } from "../theme/tokens";
import { Card } from "./Card";
import { Screen } from "./Screen";
export function NotificationsScreen() { const query = domainQueries.notifications(); const [notice, setNotice] = useState<string | null>(null); const enable = async () => { try { await enableNotifications(); setNotice("Device notifications are enabled."); } catch (cause) { setNotice(cause instanceof Error ? cause.message : "Notifications could not be enabled."); } }; const open = async (message: Record<string, unknown>) => { const payload = nestedRecord(message, "payload"); const target = firstString(payload, ["deepLink", "deep_link", "url", "route"]); if (!target) return; if (target.startsWith("/")) router.push(target as never); else if (await Linking.canOpenURL(target)) await Linking.openURL(target); }; return <Screen eyebrow="Operational updates" title="Notifications" action={<Pressable onPress={() => void enable()} style={styles.enable}><BellRing color="white" size={17} /><Text style={styles.enableText}>Enable</Text></Pressable>} refreshControl={<RefreshControl refreshing={query.isRefetching} onRefresh={() => void query.refetch()} tintColor={colors.brand} />}>{query.isPending ? <ActivityIndicator color={colors.brand} /> : query.error ? <Card><Text style={styles.error}>{query.error.message}</Text><Pressable onPress={() => void query.refetch()}><Text style={styles.link}>Retry securely</Text></Pressable></Card> : <>{(query.data ?? []).map((message, index) => { const payload = nestedRecord(message, "payload"); const status = displayStatus(message) ?? "queued"; const title = firstString(payload, ["title", "subject"]) ?? (firstString(message, ["purpose"]) ?? "SKIMA update").replace(/[_-]/g, " "); const body = firstString(payload, ["body", "message", "text"]) ?? "A backend update is available for your account."; const created = firstString(message, ["created_at", "createdAt"]); const target = firstString(payload, ["deepLink", "deep_link", "url", "route"]); return <Pressable key={recordId(message) ?? String(index)} disabled={!target} onPress={() => void open(message)} style={styles.item}><View style={[styles.icon, (status.includes("sent") || status.includes("deliver")) && styles.delivered]}>{status.includes("sent") || status.includes("deliver") ? <CheckCircle2 color={colors.success} size={22} /> : <Bell color={colors.brand} size={22} />}</View><View style={{ flex: 1, gap: 5 }}><View style={styles.row}><Text style={styles.title}>{title}</Text><Text style={styles.status}>{status.replace(/[_-]/g, " ")}</Text></View><Text style={styles.body}>{body}</Text><Text style={styles.time}>{formatDate(created)}</Text></View>{target ? <ChevronRight color={colors.muted} size={18} /> : null}</Pressable>; })}{(query.data ?? []).length === 0 ? <View style={styles.empty}><View style={styles.emptyIcon}><Bell color={colors.brand} size={32} /></View><Text style={styles.emptyTitle}>You’re all caught up</Text><Text style={styles.body}>Order, payment, approval, dispatch, and safety updates will appear here after the backend creates them.</Text></View> : null}</>}{notice ? <Text style={styles.notice}>{notice}</Text> : null}</Screen>; }
function formatDate(value: string | null) { if (!value) return "Time unavailable"; const date = new Date(value); return Number.isNaN(date.getTime()) ? value : date.toLocaleString(); }
const styles = StyleSheet.create({ enable: { flexDirection: "row", alignItems: "center", gap: 7, backgroundColor: colors.brand, paddingHorizontal: 14, paddingVertical: 10, borderRadius: radii.pill }, enableText: { color: "white", fontWeight: "900" }, item: { flexDirection: "row", alignItems: "center", gap: spacing.md, padding: spacing.lg, borderWidth: 1, borderColor: colors.border, borderRadius: radii.lg, backgroundColor: colors.surface }, icon: { width: 46, height: 46, alignItems: "center", justifyContent: "center", borderRadius: 23, backgroundColor: "#FFF0F1" }, delivered: { backgroundColor: "#E9F7EE" }, row: { flexDirection: "row", justifyContent: "space-between", gap: spacing.sm }, title: { flex: 1, color: colors.ink, fontSize: 16, fontWeight: "900", textTransform: "capitalize" }, status: { color: colors.brandDark, fontSize: 11, fontWeight: "800", textTransform: "uppercase" }, body: { color: colors.muted, lineHeight: 20 }, time: { color: colors.muted, fontSize: 12 }, empty: { minHeight: 300, alignItems: "center", justifyContent: "center", gap: spacing.md, padding: spacing.xl }, emptyIcon: { width: 70, height: 70, alignItems: "center", justifyContent: "center", borderRadius: 35, backgroundColor: "#FFF0F1" }, emptyTitle: { color: colors.ink, fontSize: 21, fontWeight: "900" }, error: { color: colors.danger }, link: { color: colors.brand, fontWeight: "900" }, notice: { color: colors.success, fontWeight: "800" } });
