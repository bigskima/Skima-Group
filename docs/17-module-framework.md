# Module Framework

Business modules are configurable plugins.

Implemented:

- module registry
- module versions
- module component bindings
- dispatch policy component bindings
- lifecycle events
- LPG module configuration
- module-backed service request lifecycle through generic runtime RPCs

Required remediation:

- E2E proof that LPG uses platform engines without LPG logic in platform core
- hosted remote gate after runtime remediation migration
