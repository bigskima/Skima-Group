{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run frontend:build",
  "installCommand": "npm ci",
  "outputDirectory": "apps/admin/dist",
  "ignoreCommand": "git diff --quiet \"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}\" \"${VERCEL_GIT_COMMIT_SHA:-HEAD}\" -- apps/admin packages scripts package.json package-lock.json vercel.admin.json",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
