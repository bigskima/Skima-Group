# Database Package

Supabase migrations live in `supabase/migrations`.

This package is reserved for generated database types, schema documentation, and database-adjacent
helpers that remain business-agnostic.
